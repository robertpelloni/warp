# MEMORY
- Existing codebase is in Rust (Warp terminal), with some Go files initialized.
- The project is ambitious and requires processing external tools iteratively.

## Repository Insights

### Just Every Code
- Rust heavy (~3000 files)
- Uses an `auto drive` orchestration and multiple CLI agent commands like `/plan`, `/code`, and `/solve`.
- Includes heavy browser integrations and local bridging.

### Pi Mono
- TypeScript heavy (~700 files).
- Multi-package architecture: `@earendil-works/pi-coding-agent`, `@earendil-works/pi-agent-core`, and `@earendil-works/pi-ai`.
- Focuses on "self-extensible coding agent" functionality with interactive CLI.
